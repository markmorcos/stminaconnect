## 1. Repo skeleton

- [ ] 1.1 Initialize root `package.json` with the project name, private: true, engines.node = ">=22"
- [ ] 1.2 Add `.gitignore` covering node_modules, `.env*` (except `.env.example`), Expo build artifacts, Supabase local data, OS files
- [ ] 1.3 Create `apps/mobile/` via `npx create-expo-app@latest -t expo-template-blank-typescript` (Expo SDK 52)
- [ ] 1.4 Create `supabase/` folder via `supabase init` (Supabase CLI)
- [ ] 1.5 Add `.nvmrc` pinning Node 22 LTS
- [ ] 1.6 Add root `README.md` with Prerequisites, Setup, and a table of Makefile targets

## 2. Environment configuration

- [ ] 2.1 Write exhaustive `.env.example` listing all variables (including ones introduced by later changes, commented with the change name)
- [ ] 2.2 Add `apps/mobile/src/config.ts` that reads env vars via `expo-constants` and throws on missing required values at boot
- [ ] 2.3 Add unit test proving `config.ts` throws if `EXPO_PUBLIC_SUPABASE_URL` is missing

## 3. Tooling

- [ ] 3.1 Add root `Makefile` with all targets listed in `project.md`, delegating to npm scripts or shell where needed; stub targets for later changes print `"introduced in change <name>"`
- [ ] 3.2 Configure Prettier at repo root (`.prettierrc`, `.prettierignore`) with the conventions in `project.md`
- [ ] 3.3 Configure ESLint at repo root with `@react-native/eslint-config` + `@typescript-eslint`; add ESLint scripts
- [ ] 3.4 Configure `tsconfig.json` at root + a mobile-specific `apps/mobile/tsconfig.json` extending it with `strict: true`, `noUncheckedIndexedAccess: true`
- [ ] 3.5 Install and configure `husky` + `lint-staged` for pre-commit formatting and linting
- [ ] 3.6 Install and configure `commitlint` with Conventional Commits rules; wire to `commit-msg` hook
- [ ] 3.7 Configure Jest via `jest-expo` preset; add a single sanity test (`it('adds numbers', ...)`) that passes

## 4. Supabase local dev

- [ ] 4.1 Commit `supabase/config.toml` (generated by `supabase init`), ensuring EU timezone default
- [ ] 4.2 Add empty `supabase/migrations/` and `supabase/seed.sql` (empty placeholder with a comment pointing to `add-person-data-model`)
- [ ] 4.3 Verify `make dev-up` brings up local Supabase + Expo dev server and `make dev-down` stops Supabase
- [ ] 4.4 Document troubleshooting notes (Docker not running, port conflicts) in the root README

## 5. PR + commit conventions

- [ ] 5.1 Add `.github/pull_request_template.md` with checklist (tests, `openspec validate`, translations if applicable)
- [ ] 5.2 Add `CONTRIBUTING.md` briefly describing the `/opsx:*` workflow and branching rules

## 6. Verification

- [ ] 6.1 Run `make typecheck`, `make lint`, `make test` on a clean clone in a temp directory — all pass
- [ ] 6.2 Run `openspec validate init-project-scaffolding` — passes
- [ ] 6.3 Manual verification: follow every scenario in `specs/setup/spec.md`
